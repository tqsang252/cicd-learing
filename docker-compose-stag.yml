version: '3'
services:          
    frontend:
        build: 
            context : .
            args:
                ENV: stag 

        container_name: frontend
        ports:
            - "80:80"
            - "443:443"
